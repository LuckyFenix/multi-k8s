apiVersion: v1
kind: Service
metadata:
  name: postgres-node-port-service
spec:
  type: NodePort
  selector:
    component: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
      nodePort: 30007